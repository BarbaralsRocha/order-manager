import React from 'react';
import { render } from '@testing-library/react';
import SnackbarContainer from './SnackbarContainer';

test('renders SnackbarContainer', () => {
  render(<SnackbarContainer />);
});
