import React from 'react';
import { render } from '@testing-library/react';
import DrawerContainer from './DrawerContainer';

test('renders DrawerContainer', () => {
  render(<DrawerContainer />);
});
