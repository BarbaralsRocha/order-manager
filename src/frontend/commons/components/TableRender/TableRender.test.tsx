import React from 'react';
import { render } from '@testing-library/react';
import TableContainer from './TableRender';
import { TableRow } from '@mui/material';
import EmptyState from '../EmptyState';

test('renders TableContainer', () => {
  // render();
  // <TableContainer
  //   columns={[{ label: 'teste' }]}
  //   cell={[1, 2].map((cell) => (
  //     <TableRow key={cell}>teste</TableRow>
  //   ))}
  //   emptyState={<EmptyState title="teste" description="teste" />}
  // />,
});
